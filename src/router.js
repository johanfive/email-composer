import React from 'react';
import { BrowserRouter } from 'react-router-dom';

import Compoz from './components/Compoz';


// History object is abstracted away, but it is there
// No routes set across the app yet, just created this file for future needs
export default (
<BrowserRouter>
  <Compoz />
</BrowserRouter>
);
